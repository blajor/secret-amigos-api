
const participant = ({name, surname, email, phone, friendName, friendSurname}) => {
    name,
    surname,
    email,
    phone,
    friendName,
    friendSurname
}

module.exports = {
    participant
}
